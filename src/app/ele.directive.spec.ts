import { EleDirective } from './ele.directive';

describe('EleDirective', () => {
  it('should create an instance', () => {
    const directive = new EleDirective();
    expect(directive).toBeTruthy();
  });
});